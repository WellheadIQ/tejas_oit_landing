---
import Layout from '../../layouts/PageLayout.astro';

// import Hero from '../../components/widgets/Hero.astro';
import Hero2 from '../../components/widgets/Hero2.astro';
// import Hero3 from '../../components/widgets/Hero3.astro';
import CallToAction from '../../components/widgets/CallToAction.astro';

import { getPermalink } from '../../utils/permalinks';

// import Features2 from '../../components/widgets/Features2.astro';
// import Features from '../../components/widgets/Features.astro';
import Content from '../../components/widgets/Content.astro';
import Steps from '../../components/widgets/Steps.astro';
// import Features3 from '../../components/widgets/Features3.astro';
// import FAQs from '../../components/widgets/FAQs.astro';

import content from '../../content/pages/content.json';

const content_solution = content.solutions_smarttank;
const { hero, content1, content2, steps } = content_solution;

const metadata = {
  title: content_solution.title,
};
---

<Layout metadata={metadata}>
  <!-- Hero Widget ******************* -->

  <Hero2
    tagline={hero.tagline}
    title={hero.title}
    subtitle={hero.description}
    image={{
      src: hero.image,
      alt: hero.alt,
    }}
  />

  <!-- <Hero3 image={{ src: hero.image, alt: hero.alt }} isReversed={hero.isReversed}>
    <Fragment slot="title">
      <span class="text-accent dark:text-white highlight"> {content_solution.hero.title}</span>
      <p>
        {content_solution.hero.title2}
        <span class="hidden xl:inline"></span>
      </p>
    </Fragment>

    <Fragment slot="subtitle">
      <span class="hidden sm:inline">
        <span class="font-semibold">Tejas Automation</span> is a subtext subtext...</span
      >
      <span class="block mb-1 sm:hidden font-bold text-blue-600">Tejas Automation: Production-ready.</span>
      Suitable for Oil and Gas Producers, etc etc etc.
    </Fragment>
  </Hero3> -->

  <!-- <Hero tagline="SmartTank Sensors">
    <Fragment slot="title">
      All your <span class="text-accent dark:text-white highlight">Tank Monitoring</span> Needs
    </Fragment>

    <Fragment slot="subtitle">
      <span class="font-semibold">SkyBitz SmartTank </span> portal makes every second count. Its fast, responsive, and intuitive
      reporting goes far beyond tank monitoring.
    </Fragment>

    <Fragment slot="image">
      <div class="relative h-0 pb-[56.25%]">
        <iframe
          width="560"
          height="315"
          src="https://www.youtube.com/embed/dsTXcSeAZq8"
          title="YouTube video player"
          allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture;"
          allowfullscreen
          class="absolute top-0 left-0 w-full h-full"
        >
        </iframe>
      </div>
    </Fragment>
  </Hero> -->

  <!-- Content Widget **************** -->

  <Content
    title ={content1.title}
    isReversed={content1.isReversed}
    isAfterContent={content1.isAfterContent}
    items={content1.items}
    image={content1.image}
  >
    <Fragment slot="content">
      <h3 class="text-2xl font-bold tracking-tight dark:text-white sm:text-3xl mb-2">{content1.slot}</h3>
    </Fragment>

    <Fragment slot="bg">
      <div class="absolute inset-0 bg-blue-50 dark:bg-transparent"></div>
    </Fragment>
  </Content>

  <!-- Content Widget **************** -->

  <Content
    isReversed={content2.isReversed}
    isAfterContent={content2.isAfterContent}
    items={content2.items}
    image={content2.image}
  >
    <Fragment slot="content">
      <h3 class="text-2xl font-bold tracking-tight dark:text-white sm:text-3xl mb-2">{content2.slot}</h3>
    </Fragment>

    <Fragment slot="bg">
      <div class="absolute inset-0 bg-blue-50 dark:bg-transparent"></div>
    </Fragment>
  </Content>

  <!-- Steps Widget ****************** -->

  <Steps
    title={steps.title}
    tagline={steps.tagline}
    isReversed={steps.isReversed}
    items={steps.items}
    image={steps.image}
  />

  <!-- CallToAction Widget *********** -->

  <CallToAction
    actions={[
      {
        variant: 'primary',
        // target: '_blank',
        text: 'Contact Us',
        href: getPermalink('/contact2'),
        // icon: 'tabler:download',
      },
    ]}
  >
    <Fragment slot="title">Be a part of our vision</Fragment>

    <Fragment slot="subtitle"> Call for action text. </Fragment>

    <Fragment slot="bg">
      <div class="absolute inset-0 bg-blue-50 dark:bg-transparent"></div>
    </Fragment>
  </CallToAction>
</Layout>
